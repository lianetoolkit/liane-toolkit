import { Meteor } from "meteor/meteor";
import { withTracker } from "meteor/react-meteor-data";
import CampaignsSettings from "/imports/ui/pages/campaigns/CampaignsSettings.jsx";

export default withTracker(props => {
  return {};
})(CampaignsSettings);
